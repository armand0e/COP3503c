#include <iostream>
#include <vector>
#include <string>
#include "LinkedList.h"
/* 
// NESTED NODE CLASS //
template <typename T> typename LinkedList<T>::Node<T>::Node() {
    
}


template <typename T> typename void LinkedList<T>::Node<T>::setSelf(T* data)
{
    this->self = data;
}

template <typename T> typename void LinkedList<T>::Node<T>::setNext(T* data)
{
    this->next = data;
}

template <typename T> typename void LinkedList<T>::Node<T>::setPrev(T* data)
{
    this->prev = data;
}

template <typename T> typename T* LinkedList<T>::Node<T>::getSelf(T* data)
{
    return this->self;
}

template <typename T> typename T* LinkedList<T>::Node<T>::getNext(T* data)
{
    return this->next;
}

template <typename T> typename T* LinkedList<T>::Node<T>::getPrev(T* data)
{
    return this->prev;
}

// LINKED LIST CLASS //
Default constructor. How many nodes in an empty list? (Answer: 0)
    What is head pointing to? What is tail pointing to? (Answer: nullptr)
    Initialize your variables!
template <typename T> LinkedList<T>::LinkedList()
{

}

Sets “this” to a copy of the passed in LinkedList. For example, if the other
    list has 10 nodes, with values of 1-10? “this” should have a copy of that
    same data.
template <typename T> LinkedList<T>::LinkedList(const LinkedList &d) 
{

}

// Delete all the nodes created by the list.
template <typename T> LinkedList<T>::~LinkedList() 
{
 
} */
